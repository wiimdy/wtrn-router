WRTN_API_KEY=replace-with-your-wrtn-api-key
WRTN_BASE_URL=https://api.wrtn.ax/api/v1
HOST=127.0.0.1
PORT=8787

# Optional: use a separate credential between CLI clients and this proxy.
# When omitted, clients authenticate with WRTN_API_KEY.
# CLIENT_API_KEY=replace-with-a-local-client-key
