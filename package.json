{
  "name": "wrtn-opencode-proxy",
  "version": "2.0.0",
  "private": true,
  "description": "Minimal Wrtn Chat compatibility proxy for OpenCode",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/proxy.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
